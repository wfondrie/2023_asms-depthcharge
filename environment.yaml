channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  # For the template:
  - python>=3.10
  - setuptools>=61.0.0
  - setuptools_scm
  - wheel
  - pip
  - python-dotenv>=0.5.1
  - pip:
      - -e . # Our local package
      - vizta
      - depthcharge-ms

  # Data Science Packages
  - umap-learn
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - scikit-learn
  - jupyterlab
  - ipykernel
  - pyarrow
  - pytorch
  - spectrum_utils
